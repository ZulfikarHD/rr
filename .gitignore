# Guest comments live only on the server (SQLite) — never commit them,
# so `git pull` never overwrites real guest messages.
/data/

# Secrets (admin password, spam settings) — upload manually, like .env
/config.php

# Editor / OS noise
.DS_Store
*.log
